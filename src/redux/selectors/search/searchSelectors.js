export const searchedVideosSelector = (state) => state.search.items;
export const isVideosLoading = (state) => state.search.isLoading;
export const isVideosLoadingError = (state) => state.search.isError;
